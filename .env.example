# Email Configuration for Hourly Reports
# Copy this file to .env and update with your actual email credentials

# Gmail Configuration (recommended)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Report Recipients (comma-separated for multiple emails)
REPORT_EMAIL=recipient1@example.com,recipient2@example.com

# Alternative Email Services:
# For Outlook: Use 'outlook' as service
# For Yahoo: Use 'yahoo' as service
# For custom SMTP: Configure host, port, secure in email_reporter.js
